setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
build:
  docker:
    release:
      dockerfile: Dockerfile
      target: production
    web: Dockerfile
  config:
    NODE_ENV: production
release:
  image: web
  command:
    - npm run migration:run